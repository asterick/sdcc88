--- a/src/SDCCmain.c
+++ b/src/SDCCmain.c
@@ -379,6 +379,9 @@
 #if !OPT_DISABLE_F8
   &f8_port,
 #endif
+#if !OPT_DISABLE_S1C88
+  &s1c88_port,
+#endif
 };
 
 #define NUM_PORTS (sizeof(_ports)/sizeof(_ports[0]))
--- a/src/port.h
+++ b/src/port.h
@@ -36,6 +36,7 @@
 #define TARGET_ID_MOS65C02 26
 #define TARGET_ID_R800     27
 #define TARGET_ID_F8       28
+#define TARGET_ID_S1C88    29
 
 /* Macro to test the target we are compiling for.
    Can only be used after SDCCmain has defined the port
@@ -66,9 +67,10 @@
 #define TARGET_IS_MOS6502  (port->id == TARGET_ID_MOS6502)
 #define TARGET_IS_MOS65C02 (port->id == TARGET_ID_MOS65C02)
 #define TARGET_IS_F8       (port->id == TARGET_ID_F8)
+#define TARGET_IS_S1C88    (port->id == TARGET_ID_S1C88)
 
 #define TARGET_MCS51_LIKE  (TARGET_IS_MCS51 || TARGET_IS_DS390 || TARGET_IS_DS400)
-#define TARGET_Z80_LIKE    (TARGET_IS_Z80 || TARGET_IS_Z180 || TARGET_IS_SM83 || TARGET_IS_R2K || TARGET_IS_R2KA || TARGET_IS_R3KA || TARGET_IS_TLCS90 || TARGET_IS_EZ80_Z80 || TARGET_IS_Z80N || TARGET_IS_R800)
+#define TARGET_Z80_LIKE    (TARGET_IS_Z80 || TARGET_IS_Z180 || TARGET_IS_SM83 || TARGET_IS_R2K || TARGET_IS_R2KA || TARGET_IS_R3KA || TARGET_IS_TLCS90 || TARGET_IS_EZ80_Z80 || TARGET_IS_Z80N || TARGET_IS_R800 || TARGET_IS_S1C88)
 #define TARGET_IS_RABBIT   (TARGET_IS_R2K || TARGET_IS_R2KA || TARGET_IS_R3KA)
 #define TARGET_HC08_LIKE   (TARGET_IS_HC08 || TARGET_IS_S08)
 #define TARGET_PIC_LIKE    (TARGET_IS_PIC14 || TARGET_IS_PIC16)
@@ -520,5 +522,8 @@
 #if !OPT_DISABLE_F8
 extern PORT f8_port;
 #endif
+#if !OPT_DISABLE_S1C88
+extern PORT s1c88_port;
+#endif
 
 #endif /* PORT_INCLUDE */
--- a/src/SDCCglue.c
+++ b/src/SDCCglue.c
@@ -832,7 +832,8 @@
 static void
 printPointerType (struct dbuf_s *oBuf, const char *name)
 {
-  _printPointerType (oBuf, name, (options.model == MODEL_FLAT24) ? 3 : 2);
+  /* a port with a true 3-byte far pointer (s1c88) emits all three bytes */
+  _printPointerType (oBuf, name, (options.model == MODEL_FLAT24 || FARPTRSIZE == 3) ? 3 : 2);
   dbuf_printf (oBuf, "\n");
 }
 
@@ -1473,7 +1474,7 @@
           dbuf_printf (oBuf, "\tret #<%s\n", name);
           dbuf_printf (oBuf, "\tret #>%s\n", name);
         }
-      else if (TARGET_IS_STM8 && FUNCPTRSIZE == 3)
+      else if ((TARGET_IS_STM8 || TARGET_IS_S1C88) && FUNCPTRSIZE == 3)   /* s1c88: 3-byte banked code pointer (lo, hi, bank) — byte 2 resolves via the (bank<<16)|logic code-address convention */
         {
           _printPointerType (oBuf, name, size);
           dbuf_printf (oBuf, "\n");
@@ -1534,11 +1535,23 @@
               dbuf_printf (oBuf, "\tret #<%s\n", val->name);
               dbuf_printf (oBuf, IN_CODESPACE (SPEC_OCLS (val->etype)) ? "\tret #>(%s + 0x8000)\n" : "\tret #0\n", val->name);
             }
-          else if (port->use_dw_for_init)
+          else if (port->use_dw_for_init && FARPTRSIZE == 2)
             dbuf_tprintf (oBuf, "\t!dws\n", val->name);
           else
             printPointerType (oBuf, val->name);
         }
+      else if (size == GPTRSIZE && size == NEARPTRSIZE && GPTRSIZE < FARPTRSIZE)
+        {
+          /* untagged 2-byte near/generic pointer on a port whose far pointer
+             is wider (s1c88): plain emission, no generic-pointer tag byte */
+          if (port->use_dw_for_init)
+            dbuf_tprintf (oBuf, "\t!dws\n", val->name);
+          else
+            {
+              _printPointerType (oBuf, val->name, size);
+              dbuf_printf (oBuf, "\n");
+            }
+        }
       else if (size == GPTRSIZE)
         {
           int type;
@@ -1723,9 +1736,11 @@
             dbuf_printf (oBuf, "\t.byte %s,%s", aopLiteral (val, 1), aopLiteral (val, 0));
           if (IS_GENPTR (val->type))
             dbuf_printf (oBuf, ",%s\n", aopLiteral (val, 2));
-          else if (IS_PTR (val->type))
+          else if (IS_PTR (val->type) && GPTRSIZE > FARPTRSIZE)
+            /* mcs51-like tagged generic pointer: byte 2 is the space tag */
             dbuf_tprintf (oBuf, ",!immedbyte\n", pointerTypeToGPByte (DCL_TYPE (val->type), val->name, sym->name));
           else
+            /* true 3-byte pointer (s1c88 far): byte 2 is the real page byte */
             dbuf_printf (oBuf, ",%s\n", aopLiteral (val, 2));
           break;
         case 4:
@@ -1762,11 +1777,23 @@
     }
   else if (size == FARPTRSIZE)
     {
-      if (port->use_dw_for_init)
+      if (port->use_dw_for_init && FARPTRSIZE == 2)
         dbuf_tprintf (oBuf, "\t!dws\n", val->name);
       else
         printPointerType (oBuf, val->name);
     }
+  else if (size == GPTRSIZE && size == NEARPTRSIZE && GPTRSIZE < FARPTRSIZE)
+    {
+      /* untagged 2-byte near/generic pointer on a port whose far pointer is
+         wider (s1c88): plain emission, no generic-pointer tag byte */
+      if (port->use_dw_for_init)
+        dbuf_tprintf (oBuf, "\t!dws\n", val->name);
+      else
+        {
+          _printPointerType (oBuf, val->name, size);
+          dbuf_printf (oBuf, "\n");
+        }
+    }
   else if (size == GPTRSIZE)
     {
       printGPointerType (oBuf, val->name, sym->name,
@@ -2046,7 +2073,19 @@
   emitRegularMap (i_abs, TRUE, TRUE);
   emitRegularMap (bit, TRUE, TRUE);
   emitRegularMap (pdata, TRUE, TRUE);
-  emitRegularMap (xdata, TRUE, TRUE);
+  if (TARGET_IS_S1C88)
+    {
+      /* the __far space is const ROM data on the S1C88: emit it as a romable
+         static segment (initializer bytes inline), never .ds + GSINIT runtime
+         stores (which would also need EP-paged addressing) */
+      if (xdata->syms)
+        {
+          dbuf_tprintf (&xdata->oBuf, "\t!areacode\n", xdata->sname);
+          emitStaticSeg (xdata, &xdata->oBuf);
+        }
+    }
+  else
+    emitRegularMap (xdata, TRUE, TRUE);
   emitRegularMap (x_abs, TRUE, TRUE);
   if (port->genXINIT)
     {
@@ -2509,7 +2548,7 @@
     }
 
   /* copy external ram data */
-  if (xdata && (mcs51_like || TARGET_MOS6502_LIKE ))
+  if (xdata && (mcs51_like || TARGET_MOS6502_LIKE || (TARGET_IS_S1C88 && dbuf_get_length (&xdata->oBuf))))
     {
       fprintf (asmFile, "%s", iComments2);
       fprintf (asmFile, "; uninitialized external ram data\n");
--- a/src/SDCCgenconstprop.cc
+++ b/src/SDCCgenconstprop.cc
@@ -120,6 +120,11 @@
       v.min = 0;
       if (IS_FUNCPTR (type))
         v.max = (1ll << ((IFFUNC_ISBANKEDCALL (type->next) ? BFUNCPTRSIZE : FUNCPTRSIZE) * 8)) - 1;
+      else if (TARGET_IS_S1C88)
+        /* the s1c88 __far pointer (3 bytes) is WIDER than the generic pointer
+           (2 bytes, == near); clamping to GPTRSIZE would mark the page byte
+           "known zero" and constant-fold it away */
+        v.max = (1ll << (getSize (type) * 8)) - 1;
       else
         v.max = (1ll << (GPTRSIZE * 8)) - 1;
       v.knownbitsmask = ~((unsigned long long)v.max);
--- a/src/SDCCopt.c
+++ b/src/SDCCopt.c
@@ -2469,7 +2469,11 @@
               else
                 {
                   nextresulttype = operandType (IC_RESULT (uic));
-                  if (!IS_INTEGRAL (nextresulttype) && !(IS_PTR (nextresulttype) && NEARPTRSIZE == 2))
+                  /* For a pointer-result use the addend may be narrowed to 16
+                     bits only if the pointer itself is 16-bit — on a port with
+                     wider pointers (s1c88 3-byte __far) the narrowing would
+                     truncate away the sign-extension into the page byte. */
+                  if (!IS_INTEGRAL (nextresulttype) && !(IS_PTR (nextresulttype) && NEARPTRSIZE == 2 && getSize (nextresulttype) == 2))
                      continue;
 
                   if (IS_PTR (nextresulttype))
--- a/src/SDCCicode.c
+++ b/src/SDCCicode.c
@@ -2416,6 +2416,27 @@
       right = geniCodeMultiply (right,
                                 operandFromLit (getSize (ltype->next)),
                                 (getArraySizePtr (left) >= INTSIZE) ? RESULT_TYPE_INT : RESULT_TYPE_CHAR);
+      /* s1c88: a 3-byte __far pointer minus a narrower signed index needs the
+         index widened (mirroring geniCodeAdd), or the sign byte is lost and
+         the page byte only sees the borrow */
+      if (TARGET_IS_S1C88 && getArraySizePtr (left) > getSize (operandType (right))
+          && !IS_UNSIGNED (getSpec (operandType (right))))
+        {
+          sym_link *type = 0;
+          switch (getArraySizePtr (left))
+            {
+            case 2:
+              type = newIntLink ();
+              break;
+            case 3:
+            case 4:
+              type = newLongLink ();
+              break;
+            default:
+              wassert (0);
+            }
+          right = geniCodeCast (type, right, TRUE);
+        }
       resType = copyLinkChain (IS_ARRAY (ltype) ? ltype->next : ltype);
     }
   else
