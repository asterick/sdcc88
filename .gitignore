# Native build (build.sh work dir: fetched SDCC tree + tarball)
/build/

# Claude Code local (personal) settings
.claude/settings.local.json

# Editor / OS
*.swp
*~
.DS_Store
.idea/
.vscode/

# scratch: raw PDF text extraction
docs/_extract/
# NB: scripts/corpus/.baseline/ is TRACKED — it's the golden byte-identity
# reference the corpus-check gate (and CI) compares against. Re-bless it with
# `scripts/corpus-check.sh snapshot` when codegen legitimately changes.
